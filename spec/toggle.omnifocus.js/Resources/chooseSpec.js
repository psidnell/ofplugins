describe('choose', () => {
    var PlugIn = require('../../mocks/PlugIn');
    //require('./choose');

    it('can test code', () => {
        var demo = new PlugIn();
        demo.print();
    });
});
